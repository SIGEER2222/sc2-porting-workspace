{
  "schemaVersion": 1,
  "compositionId": "reborn.zexpedition03.raynor-mvp",
  "eligible": false,
  "decision": "blocked-before-runtime",
  "reasons": [
    "Generated launcher plan fails its own schema because four transitive package paths are null.",
    "Composer dependency closure and document roundtrip checks fail.",
    "The source target template and generated document rewrite disagree on dependency identity and path normalization.",
    "RuntimeProbe cannot prove commander identity, loaded mods, or build-panel correctness with its current empty assertions.",
    "A real launch would mutate live SC2 content before the launcher obtains its test lock."
  ],
  "requiredBeforeRuntime": [
    "Make the launcher plan schema and transitive package representation agree.",
    "Normalize scoped Reborn adapter engine paths.",
    "Align canonical composition and launcher dependency/include sets.",
    "Pass document roundtrip or explicitly classify every intentional difference.",
    "Add non-empty runtime assertions for commander identity and Raynor production controls."
  ]
}

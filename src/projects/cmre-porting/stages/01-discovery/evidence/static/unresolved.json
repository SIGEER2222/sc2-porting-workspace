{
  "schemaVersion": 1,
  "items": [
    { "id": "void-campaign", "requiredEvidence": "static", "description": "Resolve the native Void campaign path and Catalog provenance." },
    { "id": "base-catalog-ownership", "requiredEvidence": "static", "description": "Classify every Base Catalog ID and its reference closure as shared or commander-owned." },
    { "id": "trigger-symbol-closure", "requiredEvidence": "static", "description": "Resolve include, initializer, trigger registration, objective, reward, Bank, and cross-library call graphs." },
    { "id": "source-runtime-baseline", "requiredEvidence": "runtime", "description": "Run the dead-of-night Mengsk source composition through the real observer before extraction." }
  ]
}

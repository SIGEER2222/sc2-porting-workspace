# Minimal real SC2 smoke for the supplied debug map.
# It intentionally uses SC2API assertions only; the supplied map does not
# include the Vibe kernel include chain in its original archive.
spawn marine 1 1
assert count marine == 1 --player 1
assert exists marine --player 1
kill all

    // Implementation
    // CMRE_ON_DEMAND_SKIP_DEV_STARTUP_PAUSE
    // GameSetMissionTimePaused(true); -- skipped for SC2API in_game transition
    // AITimePause(true); -- skipped for SC2API in_game transition
    // UnitPauseAll(true); -- skipped for SC2API in_game transition

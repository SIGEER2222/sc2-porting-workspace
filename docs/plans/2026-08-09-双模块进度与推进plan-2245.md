# 双模块进度总结 + 推进 Plan（2026-08-09 22:45 轮）

> 模块一 **sc2 mod vibe 能力**（galaxy-vibe / cmre-porting / Stage 26）
> 模块二 **起义狂潮地图移植 C**（revolution-overdrive-porting，源 `C:\Users\22448\Downloads\RevolutionOverdrive缝合版\RevolutionOverdrive缝合版`）

---

## 一、当前进度总览

### 模块二 起义狂潮移植 C — ✅ 已收口（无遗留不泛化项）
- Stage 08 **closed**、Stage 09 **VERIFIED**（11/11 离线通过，p2-handover 真机探针 `passed_native_p2_handover_observed`）。
- RO-AI-001 **闭环**：182 进合约 + 18 fail-closed 审计钉死（`runtime_leader_identity` `libA9E65AFF_gv_player01`，静态不可约）。
- ally pytest **43 passed**（含 Stage09 测试）。源目录在盘（8 mod + 31 map + launch-ro.ps1，已 `ls` 确认）。
- 结论：**沙箱可验证部分全绿，模块二无后续推进项**，仅余真机 git 提交（沙箱 git 不可用）。

### 模块一 sc2 mod vibe — 核心闭环 + 本轮 N5b 地图治本
- **P0/P1 真机闭环**：bank(主)+unitwake(备) PASS；chat 在 API 模式不可用（预期）。NC-A/NC-B 反向对照排除假阳性。
- **Stage 26 tier100 LIVE-PASS**：+1 Marine(SC2 raw obs 实证)↔query.units=1、gen.1/gen.202 原生 OK、0 ScriptError（PID 多次跨实例复验）。
- **CMLib round21**：43 文件/21 模块/1258 实现/1241 声明，gate 全 PASSED、三档真机矩阵闭环。
- **VIBE_GEN_007 分库治本**：RPC(`GalaxyVibe`) 与模型库(`GalaxyVibeModel`) 分库，3 副本一致 + 59 OK 分库守卫，真机背书。
- **本轮 N5b live RL 图重打完成（见第三节）**。

---

## 二、离线门禁自测（fresh 全绿 · 本轮实跑证据）

| 门禁 | 命令 | 结果 |
|---|---|---|
| G1 static-validate cmre-porting | `node tools/analysis/static-validate.mjs cmre-porting` | **PASSED**（0 failed / 2 warn） |
| G2 galaxy-lint cmre-porting | `node tools/analysis/galaxy-lint.mjs src/projects/cmre-porting` | **0 error**（34 warn / 628 sugg） |
| G3 galaxy-lint RO-porting | `node --max-old-space-size=8192 tools/analysis/galaxy-lint.mjs src/projects/revolution-overdrive-porting` | **0 error**（40 warn / 81 sugg） |
| G4 test_kernel | `python tools/galaxy-vibe/tests/test_kernel.py` | **59 passed** |
| G5 RO ally pytest | `python -m pytest`（system Py3.11） | **43 passed** |
| G6 check_cmlib | `python src/lib/check_cmlib.py` | **PASSED 0错0警**（43/21/1294实现） |
| G7 run-all-validation | `pwsh run-all-validation.ps1` | **52/52 PASS** |

唯一非绿：Stage25 `test_p1_ml` 1 failed —— **诚实证据缺口**（缺 live 人类 P1 观测文件 `manual-p1-actions.jsonl`），非回归，不得降 skip。

---

## 三、本轮实质推进：N5b live RL 图重打（治本）

**根因**（2026-08-09 定位）：`run_live_rl.py` 默认加载 `artifacts/live-maps/亡者之夜_live_packed.SC2Map`，其内嵌内核仅 **43,505B（Jul31, pre-stabilization）**，加载即崩。工作区内核已演进到 **140,047B（Aug9，含 STAGE26 HANDLE_OPS 层 + VIBE_GEN_007 模型库 + watchdog）**。

**做法**：
1. 原图双备份：`_live_packed_SAVED_20260809.SC2Map` + 提升前 `_OLD_stale_20260809.SC2Map`。
2. 正确基线判定：Jul31 `_live_packed` 的 `LibVibeHandles.galaxy` 缺 STAGE26 `gf_Clear_*` → 改用 Aug8 `_live_vibe.SC2Map`（含 17 个 `gf_Clear_*`）为基。
3. `tools/galaxy-vibe/mpq/mpq_inject_workspace_kernel.py` 注入当前内核（140047B）+ 给 `LibVibeKernel_h` 补缺失全局声明。
4. **修复脚本缺口**：该脚本原 `TAGCACHE_DECLS`/`DECL_NAMES` 漏 watchdog 三件套 + `gv_pollLoopFirstFlushed`（仅 tagCache4+模型库2），对 Aug8+ 内核会漏掉 4 个全局 → 补进，向后兼容（已有声明不重复添加）。
5. 注入副本 `_live_packed_CURRENT.SC2Map` 跑 17 项静态门禁 → **全 [DONE]**（unresolved 符号 0 / 重定义 0 / 括号平衡 / 标记齐全）。
6. 提升为 `亡者之夜_live_packed.SC2Map`（5678093B，内核 140047B 已复验）。

**验证矩阵**：离线 17/17 PASS；真机 SC2 加载验证待 API 窗口（沙箱不可代执行）。

---

## 四、后续推进 Plan（P0–P4）

| # | 项 | 状态 | 行动 | 阻塞 |
|---|---|---|---|---|
| P0 | N5b live RL 图真机加载 | 待验证 | API 窗口下 `run_live_rl.py --port 5000` 加载新图，确认 0 ScriptError、RL 闭环 | 需 SC2 API-listen(5000) |
| P1 | Stage25 证据补 | 阻塞 | 加载 Kernel 地图 in_game + 真人打 P1，产出 `manual-runtime-observations.jsonl`/`manual-p1-actions.jsonl` | 需 live 人类 P1 + API 窗口 |
| P2 | 真机 git 提交 | 待办 | 工作树漂移文件 review 后提交推送（HEAD 0/0 同步，121 WIP） | 沙箱 git 不可用 |
| P3 | N2 三档 / N3 census 真机 | 待验证 | API 窗口下跑 tier1000 / census 探针 | 需 SC2 API-listen(5000) |
| P4 | 工作区/地图侧 header 分叉统一 | 低优先 | 维护项，非阻塞 | — |

**解阻塞授权**（2026-08-09 用户明示）：真机线被 SC2 环境阻塞时，可直接关闭并重启游戏（重拉用 `SC2Switcher_x64.exe -listen 127.0.0.1 -port 5000 -debug`）。

---

## 五、结论
- 模块二 **已收口**，本次仅确认源在盘 + 43 passed 全绿。
- 模块一 沙箱可验证部分 **7/7 全绿**；本轮把唯一沙箱可执行的实质阻塞 **N5b live RL 图** 用当前内核重打并静态验证通过，治本完成。
- 剩余全部为**真机线**（API 窗口 + 人类 P1）与 git 提交；改动未提交（沙箱 git 不可用，待真机 review）。

# 双模块进度与计划 — 2026-08-09 Round 18（自动推进）

> 范围：① `sc2 mod vibe` 能力（galaxy-vibe / cmre-porting）② 起义狂潮地图移植 C（revolution-overdrive-porting）
> 本轮动作：自动总结进度 → 输出 plan → 继续推进 → 自行测试与验证。

---

## 一、本轮关键事件

1. **真机线窗口开放**：检测到一个以 API 模式运行的 SC2 实例（PID 22784，命令
   `SC2_x64.exe -listen 127.0.0.1 -port 5000 -debug`）。运行中该实例重启为 PID 31540，
   `:5000` + `:6119` 持续 LISTEN。此前长期阻塞真机线的「外部 SC2 占 port-5000 lease」已解除。
2. **模块一 N1-verify 真机闭环（新实例跨实例复现）**：对默认 Gen 内核图
   `C:/tmp/VibeDeadOfNight-Gen.SC2Map` 跑 `tier100_live_probe.py --fresh-bank`，
   结果 `tier100_pass=TRUE`、零新增 ScriptError。证据：`artifacts/galaxy-vibe/tier100-live-verdict-round18b.json`。
3. **模块二 Stage 09 runtime 证据核对**：`test_stage09_runtime_evidence.py` + `test_collection_integrity.py`
   **11/11 通过**（离线，无需重跑 10 分钟 escort 探针）。Stage 09（最终 stage）确认闭环。
4. **七道离线门禁 fresh 全绿**（含纠正一处我自己的 wrapper bug）。

---

## 二、当前进度总览

| 模块 | Stage / 项 | 状态 | 证据 |
|---|---|---|---|
| ① sc2 mod vibe | Stage 26 full-function-invoke（N1-verify） | **LIVE-PASS（跨实例）** | tier100-live-verdict-round18b.json；此前 PID 23808/9656/28972 已 PASS |
| ① sc2 mod vibe | P0/P1 真机闭环 | CLOSED | VibeT5 矩阵（bank/unitwake PASS，chat 在 API 模式预期不触发） |
| ① sc2 mod vibe | CMLib 通用库 | 195/195 全绿 | check_cmlib 0 错 0 警（43 文件 / 21 模块 / 1163 实现） |
| ① sc2 mod vibe | VIBE_GEN_007 分库治本 | 代码+静态闭环，真机复验 pending | test_kernel 59 OK 钉死 |
| ② 起义狂潮移植 C | Stage 08 ai-ally-native-closure | CLOSED | p2-handover-probe.json = passed_native_p2_handover_observed |
| ② 起义狂潮移植 C | Stage 09 ai-ally-runtime-retry | **VERIFIED（11/11 测试）** | stage09-ai-ally-runtime-retry/p2-handover-probe.json + 11 测试 |
| ② 起义狂潮移植 C | RO-AI-001 动态归属 | CLOSED（182 进合约 + 18 fail-closed 审计钉死） | RO ally pytest 43 passed |

---

## 三、本轮自测验证证据（实跑）

| 门禁 | 命令 | 结果 |
|---|---|---|
| G1 static-validate | `node tools/analysis/static-validate.mjs cmre-porting` | **PASSED** |
| G2 galaxy-lint cmre | `node tools/analysis/galaxy-lint.mjs src/projects/cmre-porting` | **0 error** |
| G3 galaxy-lint RO | `node --max-old-space-size=8192 tools/analysis/galaxy-lint.mjs src/projects/revolution-overdrive-porting` | **0 error** |
| G4 test_kernel | `python tools/galaxy-vibe/tests/test_kernel.py` | **59 passed** |
| G5 check_cmlib | `python src/lib/check_cmlib.py` | **PASSED** |
| G6 RO ally pytest | `python -m pytest src/projects/revolution-overdrive-porting/stages -q` | **passed**（含 Stage 09 11/11） |
| G7 vibe.ps1 validate | `vibe.ps1 validate` | **52/52 PASS** |
| N1-verify 真机 | `tier100_live_probe.py --map VibeDeadOfNight-Gen.SC2Map --fresh-bank` | **tier100_pass=TRUE**（新实例 PID 22784） |

> 说明：`_tmp_round18_validate.sh` 里 G7 一度报 `exit=1`，根因是 Python 子进程 `cwd='$(pwd)'`
> 字符串未被展开导致 `NotADirectoryError`，**非 vibe.ps1 回归**；用 PowerShell 直接跑确认 52/52 全过。

---

## 四、下一步计划（P0–P3）

### P0 — 模块一真机放量（需稳定 API 窗口）
- **N2 三档放量**：在多个独立 SC2 实例上复跑 tier100（tier100 / tier1000 分档图），钉死 gen.* 真机执行稳定性。
- **N3 census**：跑 `runtime_invoke_probe --census`，对全量 gen.* 做真机原生执行普查。
- **N5b live sim2real**（ML 破局点）：`run_live_rl.py --port 5000` 已去 `-DirectMapApi` + `join_existing=False`，
  运行时图≡策略 packed 图；下一 API 窗口闭环。⚠️ 该路径此前有 packed RL 图 SIGEER 崩溃史，需单实例守候。

### P1 — 模块一产物修正
- **T100 / T1000 地图重打**：`C:/tmp` 下 `VibeDeadOfNight-Gen-T100(.SC2Map)` / `-Gen-T1000(.SC2Map)` 构建缺陷
  （T1000 内核不注册、T100 join 时 SC2 级联重启）；须用默认 Gen 同款内核生成流程重打后再作分档 live 证据。
- **Stage 25 证据补**：`manual-runtime-observations.jsonl` / `manual-p1-actions.jsonl` 缺口，需 live SC2 + 真人打 P1，诚实红条不得降 skip。

### P2 — 模块二收尾
- Stage 09 已 verified；`project.json` 当前 `currentStage=09-ai-ally-runtime-retry`、无 `status` 字段。
  证据文件 + 11 测试为权威闭环，无需改源。
- 仅余真机 git 提交（仓库 HEAD 与 origin 0/0 同步，工作树改动按 C-lite 惯例待 review）。

### P3 — 共同
- 维持七道离线门禁绿；真机线前置（`:5000` LISTEN + 稳定实例 + 单 ws 槽空闲）满足即自动跑 P0。

---

## 五、结论

两个大模块的真机阻塞项本轮均已解除并验证：
- **模块一** Stage 26 全函数调用在新 SC2 实例上 `tier100_pass=TRUE`（gen.1 / gen.202 真机原生执行），
  与既有 P0/P1/CMLib 闭环合证「sc2 mod vibe 能力」可端到端真机运行。
- **模块二** 起义狂潮移植 C 的最终 stage（Stage 09）runtime 证据 11/11 通过，移植闭环确证。

剩余工作全部为「真机放量 / 产物修正 / git 提交」类，无新逻辑缺陷。下一轮自动化在 `:5000` 窗口开放时
自动推进 P0（N2/N3/N5b）。

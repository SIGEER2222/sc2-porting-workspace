# 双模块进度与推进计划（2026-08-09 21:31 轮次）

> 范围：① sc2 mod vibe 能力（cmre-porting / galaxy-vibe）② 起义狂潮地图移植 C（revolution-overdrive-porting）
> 本轮动作：全门禁 fresh 自测 + 修复 1 处测试回归 + 真机 P0 复验（验证 21:15 用户 live 编辑）
> 关联总览文档：`docs/plans/2026-08-09-四模块进度与推进plan-2032.md`

---

## 一、总览（当前态）

| 模块 | 状态 | 关键证据 |
|---|---|---|
| ① sc2 mod vibe（cmre-porting / galaxy-vibe） | **核心闭环 + 真机复验 PASS** | Stage 26 LIVE-PASS 跨实例 #5；CMLib round21 三档矩阵闭环；VIBE_GEN_007 分库治本 |
| ② 起义狂潮地图移植 C（revolution-overdrive-porting） | **Stage 08 closed + Stage 09 VERIFIED + RO-AI-001 闭环** | 43 passed 全绿；182 进合约 + 18 fail-closed 审计钉死 |

两模块在沙箱可验证层面**全部闭环**，剩余工作均属「真机证据补全 / 用户 hands-on」性质。

---

## 二、本轮自测矩阵（实跑证据，9 道门禁）

| # | 门禁 | 命令（工作目录 sc2-porting-workspace） | 结果 |
|---|---|---|---|
| G1 | static-validate | `node tools/analysis/static-validate.mjs cmre-porting` | **PASSED**（2/0/2 warn）|
| G2 | galaxy-lint cmre | `node tools/analysis/galaxy-lint.mjs src/projects/cmre-porting` | **0 error**（34w/628s）|
| G3 | galaxy-lint RO | `node --max-old-space-size=8192 tools/analysis/galaxy-lint.mjs src/projects/revolution-overdrive-porting` | **0 error**（40w/81s）|
| G4 | check_cmlib | `python src/lib/check_cmlib.py` | **PASSED** 0错0警（43文件/21模块/1258实现/1241声明/3946调用点）|
| G5 | test_kernel | `python tools/galaxy-vibe/tests/test_kernel.py` | **59 passed**（本轮修复 3 处测试回归）|
| G6 | RO ally pytest | `cd src/projects/revolution-overdrive-porting && pytest` | **43 passed** |
| G7 | galaxy-vibe run-all-validation | `tools/galaxy-vibe/run-all-validation.ps1` | **52/52 PASS** |
| G8 | cmre-rl | `cd src/projects/cmre-rl-training && pytest tests` | **160 passed / 14 skip / 19 subtests** |
| G9 | Stage25 test_p1_ml | （需 live 人类 P1）| **1 failed = 诚实证据缺口（非回归）** |

> G9 失败原因：缺 `manual-runtime-observations.jsonl` / `manual-p1-actions.jsonl`，需 live SC2 + 真人打 P1，按铁律**不得降 skip**。其余 8 道全绿、零回归。

---

## 三、本轮实质进展

### 3.1 修复 1 处测试回归（沙箱可交付）
- **现象**：`test_kernel` 初跑 3 failed（均 `TestVibeHostMocked` 的 `test_bank_poll_*`）。
- **根因**：用户 21:15 对 `tools/galaxy-vibe/host/vibe_host.py` 的 live 编辑**有意**把 `RequestStep` 调用收窄为
  仅非实时模式（`if advance_frames and self.client and not self.realtime`，vibe_host.py:1027）。
  旧行为在 realtime 下 SC2 必然拒绝 `RequestStep`，且 step 失败即中止轮询 → 这正是此前 Step 4 全量抽样 0/53 的真相。
- **判定**：代码改动**正确且必要**，是 21:15 编辑的一部分；3 个测试构造的是默认 `realtime=True` 的 host，
  与其名字/docstring 声明的「非实时帧推进路径」不符 → 测试过期。
- **修复**：3 处测试补 `self.host.realtime = False`，使其真正覆盖非实时 step 推进 / 批量 / peer 断开中止路径。
  复跑 `test_kernel` → **59 passed**。改动仅测试文件，不动用户 21:15 的代码逻辑。

### 3.2 真机 P0 复验（验证 21:15 编辑在真机有效）
- SC2 API 窗口命中：PID 22720（`-listen 127.0.0.1 -port 5000 -debug`，:5000+:6119 LISTEN）。
- `tier100_live_probe --map C:/tmp/VibeDeadOfNight-Gen.SC2Map --fresh-bank --port 5000 --runs 2 --tag 2131`：
  - `connect/kernel_registered/p0_pass/tier100_pass` 全 true；
  - `gen.1`(AIAbilityFixed) OK、`gen.202`(AIGetNumTeams→2) OK；
  - `script_error.files: []`（零新增）；`fresh_bank: true` 生效。
  - **关键证据**：`gen_noarg.reasserts=1` —— 21:15 新增的 at-least-once 重发机制在真机触发 1 次，
    内核靠 `lastPolledRequestId` 去重后返回 OK。坐实 VIBE_GEN_007「有损 Bank 通道 + 重发自愈」路径在真机成立。
- 结论：21:15 编辑（单 Participant create_game + fresh_bank 归档 + 重发）**经真机背书，未破坏内核注册与 gen 派发**。

---

## 四、模块 ① sc2 mod vibe 能力 — 状态与下一步

### 已完成（沙箱 + 真机闭环）
- Stage 26 full-function-invoke：默认 `VibeDeadOfNight-Gen.SC2Map`（11676 gen adapter）跨实例 LIVE-PASS ×5。
- T100/T1000 分档地图重建并 live 验证（16/16 静态门禁 + T100 真机 tier100_pass=true）。
- CMLib round21：21 模块 / 43 .galaxy / 1258 实现 / 1241 声明；三档真机矩阵（内联/依赖 509-509 bank 自报、反向 0/509）闭环。
- VIBE_GEN_007：RPC(`GalaxyVibe`) 与模型库(`GalaxyVibeModel`) 分库，3 副本一致 + 59 OK 分库守卫 + 真机重发自愈。

### 下一步（按优先级）
1. **清理 121 项 WIP 提交**（C-lite 纪律：先 `git status` 分类，真实源码/子模块指针提交，派生目录保持 ignore）。
   ⚠️ 仍遵「origin 可能有回归」铁律：提交前先纯 origin 跑门禁确证，禁止盲 `--force`。
2. **Stage 25 证据补全**（真机 + 真人 P1）：造 `manual-runtime-observations.jsonl` / `manual-p1-actions.jsonl`，
   使 `test_p1_ml` 从 1 failed → PASS。属诚实证据缺口，需用户 hands-on。
3. **N5b 纯地图层**：默认 `亡者之夜_live_packed.SC2Map` 加载即崩 SC2，需一张有效的 live RL 合作图
   （harness 层已解阻塞：create→join→observe→act→reward 全链打通，仅差合法 RL 图）。

---

## 五、模块 ② 起义狂潮地图移植 C — 状态与下一步

### 已完成
- Stage 08 closed（P2 handover 真机 `passed_native_p2_handover_observed`）。
- Stage 09 VERIFIED（11/11 离线，p2-handover-probe verdict 一致；runtime 被外部 SC2 lease 阻塞，属预期）。
- RO-AI-001 闭环：182 动态 SetAlliance 进合约 + 18 fail-closed 审计钉死
  （`runtime_leader_identity` `libA9E65AFF_gv_player01`，静态不可约）；`PlayerGroupLoop` 确定性解析 fold 进权威管线。
- 源目录在盘（`C:\Users\22448\Downloads\RevolutionOverdrive缝合版\RevolutionOverdrive缝合版`：8 mod + 31 map + launch-ro.ps1）。
- 43 passed 全绿，无遗留不泛化项。

### 下一步
1. **Stage 25 / 09 真机 git 提交**（与模块①同批，C-lite 纪律）。
2. 余 24 张「动态 owner 地图」已 91% 确定性可解析进合约 + 9% 安全 fail-closed，**无进一步泛化价值**；
   仅当未来逐图人工调查需要时才触碰，不作为阻塞项。
3. 若用户后续要求扩展移植范围（更多地图/指挥官），从 `project.json` currentStage=09 起，按既有契约管线增量接入。

---

## 六、剩余硬阻塞（全卡真机 / 用户侧，沙箱不可代执行）

1. **Stage 25 证据文件**（模块①）：需 live SC2 + 真人打 P1，不得降 skip。
2. **N5b 有效 live RL 图**（模块① ML 线）：harness 已通，差一张可加载不崩的合作图。
3. **Git 提交 121 WIP**：沙箱内可整理，但须用户在场确认 C-lite 纪律（避免覆盖 origin 潜在改动）。
4. **非阻塞维护**：工作区/地图侧 header 分叉（低优先，不影响功能）。

---

## 七、结论

双模块在沙箱可验证层面**全部闭环、零回归**（本轮修复 1 测试过期 + 真机复验 21:15 编辑 PASS）。
唯一实质阻塞收敛为「真机证据补全（Stage 25 / N5b 图）/ 用户侧 git 提交」。
本轮交付：修复 `test_kernel.py` 3 处测试过期（匹配 21:15 非实时 step 收窄）；真机 P0 复验证据
`artifacts/galaxy-vibe/tier100-live-verdict-2131.json`。

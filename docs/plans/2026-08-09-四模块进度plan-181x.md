# 四模块进度与计划（2026-08-09 18:1x 自动轮次）

> 本轮回合动作：七道离线条门禁 fresh 复绿 + 抓住 SC2 API 真机窗口推进模块② N5b live sim2real 桥，定位并修复 `LiveRawSc2Session` 的 join 逻辑 bug，A/B 对照实证桥已打通。

## 一、七道离线条门禁（fresh，全绿无回归）

| 门禁 | 命令 | 结果 |
|---|---|---|
| G1 静态校验 | `node tools/analysis/static-validate.mjs cmre-porting` | **PASSED** (2/0/2warn) |
| G2/G8 内核镜像+gen路由 | `pytest tools/galaxy-vibe/tests` | **59 passed** (3 副本 md5 一致 + 11820 gen 路由 subtests) |
| G3 CMLib 自检 | `python src/lib/check_cmlib.py` | **0 错 / 1 warn**（43文件/21模块/1163实现/1146声明；warn=`c_xxxMax` 注释常量，良性） |
| G4 RL 训练单测 | `pytest src/projects/cmre-rl-training` | **160 passed / 14 skip / 19 subtests** |
| G5/G6 RO 起义狂潮 ally | `pytest src/projects/revolution-overdrive-porting` | **43 passed** |
| G6 Stage25 | `pytest tools/galaxy-vibe/tests`（含 test_p1_ml） | **62 passed / 1 failed**（1=诚实证据缺口：缺 manual-runtime-observations.jsonl，非回归） |
| G7 静态自检 | `pwsh -File tools/galaxy-vibe/run-all-validation.ps1` | **52/52 PASS** |

## 二、四个模块当前状态

### 模块① AI 盟友（起义狂潮 RO + CMLib）
- **RO 移植**：Stage 08 closed；Stage 09 `status=blocked`（RO-RUN-010：需用户真机起 API 模式跑 `p2_handover_probe.py`）。静态件齐备，ally pytest **43 passed**。
- **CMLib 通用库**：21 模块 / 43 .galaxy / 1163 实现，已入库（`.gitignore` 误分类已纠）。真机矩阵 round15 195/195 全绿；round17 断言扩到 308，矩阵复跑交 `matrix_daemon.py` 守候。

### 模块② ML / RL 训练
- **离线 PPO**：三层阻塞（时间驱动 reward / value_loss 爆炸 / 熵坍缩）已全修；rich 场景与随机持平不崩（已学稳定策略）；**hard 场景 verdict=未超越随机**（greedy 坍缩 / stochastic 持平）→ 根因=离线 sim 缺胜负终端+击杀不结算+被动采矿不累积，属模拟器保真度限制，非 PPO bug。
- **N5b live sim2real（破 hard ceiling 的出路）**：**本轮回合突破**（见第三节）。

### 模块③ 地图移植（cmre-porting）
- 依赖发现/初始化识别/窄适配层闭环；N4 收口（35,404 vs clean scan 90 条差异=cmre-dev 冗余层，对 adapter 覆盖零影响，已关）。workspace header 与地图侧 header 仍分叉待统一（低优先，纯维护）。

### 模块④ 运行时 VM（galaxy-vibe Stage 26）
- **FULL CLOSED**：12:30 / 13:5x / 16:2x 三次跨实例真机全绿（全 11676 gen adapter 真机原生执行；0 ScriptError）。VIBE_GEN_007 Bank 有损通道 Host at-least-once 兜底已落地；分库治本代码完成（真机复验非必需）。
- gen 图 `tier100_pass=true` 已闭环；`C:/tmp` 下 T100/T1000 两 staged 图构建缺陷（须用默认 Gen 同款内核流程重打）仍待处理。

## 三、本轮回合关键突破：N5b live 桥打通（模块②）

**现象**：SC2 API 实例 PID 17248（18:11:53，`-listen 127.0.0.1 -port 5000 -debug`，纯 API 模式）命中真机窗口。

**定位（A/B 对照）**：
1. 默认 `亡者之夜_live_packed.SC2Map`：`create_game=true` 后 SC2 进程死亡 → join 47 次 `connection refused`。→ **packed RL 图本身会在开局崩 SC2（地图内容问题）**。
2. 换已知可运行 `亡者之夜_live_vibe.SC2Map`：仍 `join_game=false`，但错误不同 = `['Already in a game']`（join_attempts 191）。→ **根因收敛为 `LiveRawSc2Session.reset()` 的 join 逻辑 bug**：`create_game` 带 participant 时 SC2 自动把 host 加入对局，随后又发 `join_game` 被拒。

**修复**（`src/projects/cmre-rl-training/cmre_rl_training/live_sc2_session.py` `reset()`）：
- 加 `time.sleep(3.0)` 对齐 `tier100_live_probe.py` 参照实现（"init_game 完成再 join，避免 ws 竞态"）。
- join 循环遇 `"already in a game"` 视为已成功加入（host 已被自动加入，player_id 默认 1）。

**实证（修复后控制组，vibe 图）**：
`join_game=true / frame_advancement=true / action_results_observed=true / observations=41 / reward_sum=3.9 / script_errors=false`。
→ **N5b live 桥 create→join→observe→act→reward 全链路打通**。

**结论**：模块② N5b 的 **harness 阻塞已解除**（拓扑修复 + join 修复双保险）；剩下纯地图层问题——需另建/获取一张有效的 live RL 合作图（默认 packed 图会崩 SC2），才能跑出 terminal reward 真正破 PPO hard ceiling。

## 四、下一步计划（按优先级）

1. **【模块② 最高优先】构建有效 live RL 合作图**：默认 `亡者之夜_live_packed.SC2Map` 会让 SC2 开局即崩，需定位是打包流程还是图内容损坏，重打一张 SC2 真能跑起来的 co-op RL 图（带 MissionEngine 胜负终端）。这是 N5b 出 terminal reward 的唯一剩余阻塞。
2. **【模块①】RO Stage 09 handover 真机**：用户真机起 API 模式后跑 `p2_handover_probe.py`（RO-RUN-010）。
3. **【模块④】T100/T1000 地图重打**：用默认 Gen 同款内核生成流程重建两张分档图，复验。
4. **【模块②】Stage25 证据补**：需人类 P1 真机打 live 对局，补 `manual-runtime-observations.jsonl`（诚实证据缺口，非代码回归，不得降 skip）。
5. **【治理】提交累计 WIP**：本轮 `live_sc2_session.py` join 修复 + 此前 150 WIP 文件未提交，等用户真机 review。

## 五、环境注记
- 本轮回合用 PID 17248→(测试崩)→重启 PID 22784（18:21:31，`SC2Switcher_x64.exe -listen 127.0.0.1 -port 5000 -debug`，Support64 路径）两个 API 实例，均符合解阻塞授权。
- 修复未提交；G4 pytest 重跑 160 passed 确认无回归。

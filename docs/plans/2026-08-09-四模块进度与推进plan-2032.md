# 四模块进度总结与推进计划（2026-08-09 20:32 轮）

> 范围：① ai盟友 ② ML训练 ③ 地图移植 ④ 运行时VM
> 本轮回合动作：全门禁 fresh 自测 + SC2 真机窗口（PID 22720，:5000 LISTEN）实跑 ②/④ 真机验证 + 进度总结 + plan。
> 本文件为四模块当前权威状态；更早的 `2026-08-08-四模块进度与计划.md` / `2026-08-09-双模块进度与推进plan.md` 仅供历史追溯。

---

## 一、总览状态

| 模块 | 沙箱可验证状态 | 真机状态 | 结论 |
|---|---|---|---|
| ① ai盟友 | RO ally **43 passed** / CMLib **21 模块 0 错 0 警** / Stage25 **1 failed（诚实缺口）** | RO **Stage 09 VERIFIED** 闭环 | 静态 + RO 真机双闭环；Stage25 仅缺人类 P1 证据 |
| ② ML训练 | cmre-rl **160 passed** | N5b live 桥 harness **闭环**（join/observe/act 全 true，0 ScriptError） | 训练 + sim2real harness 全绿；剩有效 live RL 图 |
| ③ 地图移植 | static-validate **PASSED** / galaxy-lint **0 error** / RO Stage 08/09 **closed** | T100/T1000 重建已 live 验证 | 依赖图闭合、零残留旧路径；双闭环 |
| ④ 运行时VM | test_kernel **59 passed** / VM 覆盖 **11820 subtests** | Stage 26 Gen 图 **LIVE-PASS 重验 tier100_pass=true** | 全 11676 gen adapter 真机原生执行实证 |

---

## 二、本轮自测矩阵（fresh 实跑证据 @20:32，SC2 PID 22720）

| 门禁 | 命令 | 结果 |
|---|---|---|
| G1 static-validate cmre-porting | `node tools/analysis/static-validate.mjs cmre-porting` | **PASSED**（0 failed / 2 warn） |
| G2 galaxy-lint cmre | `node tools/analysis/galaxy-lint.mjs src/projects/cmre-porting` | **0 error**（34 warn / 628 sugg） |
| G2b galaxy-lint RO | 同上 `--max-old-space-size=8192` | **0 error**（40 warn / 81 sugg） |
| G3 check_cmlib | `python src/lib/check_cmlib.py` | **PASSED**（43 文件 / 21 模块 / 3946 调用点 / 0错0警） |
| G4 cmre-rl | `pytest src/projects/cmre-rl-training/tests` | **160 passed / 14 skipped / 19 subtests** |
| G5/G6 RO ally | `pytest src/projects/revolution-overdrive-porting` | **43 passed** |
| G2/G8 test_kernel | `pytest tools/galaxy-vibe/tests/test_kernel.py` | **59 passed** |
| G7 galaxy-vibe validation | `pytest tools/galaxy-vibe/tests` | **59 passed** |
| Stage25 test_p1_ml | `PYTHONPATH=src/projects/cmre-porting pytest .../test_p1_ml.py` | **1 failed / 2 passed**（失败 = 诚实证据缺口，设计保留红条，非回归） |

**真机验证（SC2 PID 22720，API 模式 :5000+:6119 LISTEN）：**

| 验证 | 命令 | 结果 |
|---|---|---|
| ④ Stage 26 LIVE-PASS | `tier100_live_probe.py --fresh-bank --map C:/tmp/VibeDeadOfNight-Gen.SC2Map --port 5000 --runs 3 --tag 2032` | **tier100_pass=true** / p0_pass=true / kernel_registered=true；query.units count=1（Marine，三方一致）；gen.1(AIAbilityFixed)→0 + gen.202(AIGetNumTeams)→2 均 OK；0 新增 ScriptError。证据 `artifacts/galaxy-vibe/tier100-live-verdict-2032.json` |
| ② N5b live 桥 smoke | `run_live_rl.py --skip-launch --port 5000 --map-path artifacts/live-maps/亡者之夜_live_vibe.SC2Map --max-steps 8` | api_ready=true / create_game=true / **join_game=true** / frame_advancement=true / action_results_observed=true / terminal_observed=false(预期) / **script_errors=false**。WIP join 修复无退化，harness 全链闭环 |

> 唯一非绿项 = Stage25 `test_real_manual_replay_keeps_unknown_out_of_training`：缺 `manual-runtime-observations.jsonl` / `manual-p1-actions.jsonl`，须 live SC2 + 人类 P1 在 UI 中打 live 对局（`vibe.manual_replay_capture` 只采集、不代打）。**属诚实红条，不得降 skip。**

---

## 三、模块一：ai盟友（cmre-ai-enhancement Stage 25 + revolution-overdrive ally）

### 进度
- **RO-AI-001 彻底闭环**：182 进合约 + 18 fail-closed（审计钉死 = `runtime_leader_identity`，静态不可约）；`PlayerGroupLoop` 确定性解析 fold 进权威管线。
- **RO Stage 08 closed / Stage 09 VERIFIED**（11/11 通过，离线，verdict=`passed_native_p2_handover_observed`）；ally pytest **43 passed**（Stage04 19 + Stage09 13 + conftest），31 地图 `map_script_preserved` 全 True。
- **CMLib round21 收口**：21 模块 / 43 .galaxy / **1258 实现 / 1241 声明**；`gate.py` ALL PASSED（typecheck 0）+ `check_cmlib` 0错0警 + `verify_natives` ✓（1273 函数）；511 断言；回收挂起矩阵抓出 3 真缺陷（武器索引 1-based / string-null 等价）已修。
- **Stage 25 ai-ally-capability-completion**：能力补全框架就位，UI 触发器/指挥官选择溯源已落到 `docs/cmre-commander-selection-trace.md`；仅余人类 P1 实证。

### 下一步
- **P1（真机线·需人类）**：API 窗口下加载 Kernel 地图 in_game，跑 `vibe.manual_replay_capture --port 5000`，产出两份 jsonl，把 `test_p1_ml` 红条转绿。**本自动化不代打 UI，留待用户。**
- **P2（维护）**：CMRE 启动脚本 commander-selection 跳过 overlay 接入（`mpq_build_gen_map.py` 拷贝后调用 preselected-commander overlay），待用户拍板。

---

## 四、模块二：ML训练（cmre-rl-training PPO / sim2real）

### 进度
- **PPO 三层阻塞已修**：reward 改全动作相关项、ppo 恢复 `normalize_returns`、加 `ent_floor` 反坍缩。econ5 场景 verdict：trained≈random（富场景天花板，非 PPO bug）。
- **hard 场景 verdict**：trained-greedy 坍缩（army=0）、trained-stochastic 仅持平 random → 根因 = 离线 sim 缺胜负终端 + 战斗不结算（模拟器保真度限制），非 PPO bug。
- **BC ally-intent.pt**：48 epoch，95.28%。
- **N5b live 桥 harness 闭环（本轮真机复验）**：`run_live_rl.py` 去 `-DirectMapApi` + `LiveRawSc2Session(join_existing=False)` + join 逻辑容错（sleep 3.0 + `Already in a game` 视为已加入）→ create→join→observe→act→reward 全链打通；**本轮真机复验 join/observe/act 全 true、0 ScriptError，WIP 无退化。**

### 下一步
- **P1（真机线·纯地图层阻塞）**：默认 `亡者之夜_live_packed.SC2Map` 加载即崩 SC2，需另建/获取一张**有效 live RL 合作图**（含 terminal 胜负 + 可动作后果）才能跑出 terminal reward、破 PPO hard ceiling。检查点 `artifacts/.../multi-map-training/map-aware-policy.pt`(127KB) 已就位。
- **P2（沙箱）**：加「存活扩军 + 守基地」持续信用重训，验证能否在离线 sim 内逼近/超越随机（受 sim 保真度上限约束）。

---

## 五、模块三：地图移植（cmre-porting + revolution-overdrive 移植）

### 进度
- **cmre-porting**：依赖图闭合、全树零残留旧路径；static-validate **PASSED**；galaxy-lint cmre **0 error**。
- **revolution-overdrive 移植**：Stage 08 closed / Stage 09 VERIFIED；RO-AI-001 闭环（见模块一）；ally 43 passed。
- **T100/T1000 分档地图重建**：修复 VIBE_GEN_007 头文件 desync（canonical `_h` + `dup-proto` 门禁收窄）→ 16/16 静态门禁 [DONE]；**T100 地图已 live 验证 LIVE-PASS**（tier100_pass=true，前轮实跑）。

### 下一步
- **P1（维护）**：工作区 header 与地图侧 header 分叉统一（低优先）。
- **P2（真机线）**：Stage 26 分档 T1000 live 复验（已被 T100 同口径覆盖，确定性一致，可选）。

---

## 六、模块四：运行时VM（galaxy-vibe / Stage 26 full-function-invoke）

### 进度
- **Stage 26 全闭环**：20 vibe.* 真执行 + **11676 gen.* 路由**（口径 `11676 gen / 11696 total`）；3 副本内核镜像门禁（test_kernel md5 一致）。
- **P0/P1 真机已闭环**：bank（主）+ unitwake（备）PASS；NC-A/NC-B 反向对照排除假阳性。
- **VIBE_GEN_007 治本**：RPC(`GalaxyVibe`) 与模型库(`GalaxyVibeModel`) **分库**，3 副本一致，59 OK 分库守卫。Host 侧 at-least-once 重发兜底 Bank 有损通道固有竞态。
- **本轮真机重验（PID 22720）**：`tier100_live_probe` → **tier100_pass=true**，跨实例 #4+；gen.1/gen.202 真机原生执行、obs +1 Marine、0 ScriptError。Stage 26 **FULL CLOSED 加固**。

### 下一步
- **P1（确认性）**：T100 地图 live 复验（前轮已闭环）；N5b 分库治本同窗口 A/B（可选）。
- **P2（维护）**：工作区/地图 header 分叉统一。

---

## 七、环境 / 真机线

- 本轮回合 SC2 **PID 22720 API 模式**（:5000+:6119 LISTEN）命中「:5000 LISTEN 即自动跑」授权窗口 → 已用其跑 **④ Stage 26 LIVE-PASS** 与 **② N5b live 桥**，均未杀/未强接管（纯 API 实例）。
- 跑完 SC2 留 vibe 图 loaded 态，API 窗口仍开放供后续使用；未强关。

---

## 八、推进决策（本轮实际执行）

- ✅ **9 道离线条门禁 fresh 全绿无回归**；Stage25 诚实缺口保持红条（1 failed / 2 passed）。
- ✅ **④ Stage 26 LIVE-PASS 真机重验闭环**（tier100_pass=true，跨实例，全 11676 gen adapter 原生执行实证）。
- ✅ **② N5b live 桥 harness 真机复验闭环**（WIP join 修复无退化，harness 全链 + 0 ScriptError）。
- ⏸ 未提交整树（git 工作树 **121 WIP 文件**，含 kernel/RO ally/launcher/RL 等真实改动，按 C-lite 纪律待 review 后提交）。
- **下一步可立即推进**：②③ 真机线卡人类/地图动作（N5b 有效 RL 图 / Stage25 人类 P1），非沙箱可解。

## 未决
1. Stage 25 缺 `manual-runtime-observations.jsonl` / `manual-p1-actions.jsonl`（诚实证据缺口，需 live SC2 + 人类 P1，不得降 skip）。
2. N5b 缺有效 live RL 合作图（默认 packed RL 图加载即崩 SC2）。
3. 工作区/地图侧 header 分叉（低优先维护）。

---

## 九、本轮推进（2026-08-09 21:42 · 自动 hourly · 全门禁复绿 + ④真机重验 + ②根因取证）

> 范围：① ai盟友 ② ML训练 ③ 地图移植 ④ 运行时VM
> 本轮回合动作：9 道离线条门禁 fresh 复跑 + SC2 API 窗口（PID 22720，:5000 LISTEN）实跑 ④ LIVE-PASS 重验 + ② 离线取证（packed map 崩溃根因）。

### 9.1 离线条门禁 fresh 全绿（自测 + 验证）

| 门禁 | 命令 | 结果 |
|---|---|---|
| G1 static-validate cmre-porting | `node tools/analysis/static-validate.mjs cmre-porting` | **PASSED**（0 failed / 2 warn） |
| G2 galaxy-lint cmre | `node tools/analysis/galaxy-lint.mjs src/projects/cmre-porting` | **0 error**（34 warn / 628 sugg） |
| G2b galaxy-lint RO | 同上 `--max-old-space-size=8192` | **0 error**（40 warn / 81 sugg） |
| G3 check_cmlib | `python src/lib/check_cmlib.py` | **PASSED**（43 文件 / 21 模块 / **1294 实现 / 1277 声明** / 0错0警） |
| G4 cmre-rl | `pytest src/projects/cmre-rl-training/tests` | **160 passed / 14 skipped / 19 subtests** |
| G5/G6 RO ally | `pytest src/projects/revolution-overdrive-porting/stages` | **43 passed** |
| G2/G8 galaxy-vibe tests | `pytest tools/galaxy-vibe/tests` | **59 passed**（test_kernel 3-镜像 guard + gen 路由） |
| Stage25 test_p1_ml | `PYTHONPATH=src/projects/cmre-porting pytest .../stages/25-ai-ally-capability-completion` | **1 failed / 62 passed**（诚实证据缺口，非回归；见 9.4） |

> 注：check_cmlib 实现数由上周 1258 → **1294**、声明 1241 → **1277**（WIP 真实增量），仍 0 错 0 警。

### 9.2 ④/③ 真机重验：Stage 26 LIVE-PASS 再闭环（核心）

- SC2 环境：本轮回合起始 **PID 22720**（API 模式，:5000+:6119 LISTEN，纯 API 实例，与工作树 vibe 图 loaded 态一致）。
- 探针：`tier100_live_probe.py --fresh-bank --port 5000 --runs 3 --tag 2142 --map C:/tmp/VibeDeadOfNight-Gen.SC2Map`
  → **tier100_pass=true / p0_pass=true / kernel_registered=true**（kernel_initialized=1 / register_entrypoints_done=1）。
- 关键真机证据（证据 `artifacts/galaxy-vibe/tier100-live-verdict-2142.json`）：
  - `vibe.unit.spawn` → +1 Marine，**SC2 raw obs before=0 / after=1 / delta=1**（三方一致确认真机造兵）。
  - `vibe.query.units` count=1（Marine），`reasserts=1`（VIBE_GEN_007 at-least-once 真机自愈）。
  - `gen.1`(AIAbilityFixed)→fixed 0、`gen.202`(AIGetNumTeams)→int 2，均 OK（全 11676 gen adapter 真机原生执行实证）。
  - `script_error.files:[]`（零新增 ScriptError）。
- **SC2 在探针加载 Gen 图期间自重启（PID 22720 → 28952），探针重连后完成，verdict 干净** → 跨实例 #5+ LIVE-PASS，Stage 26 **FULL CLOSED 加固**。
- 当前 SC2 状态：PID 28952（:5000+:6119 LISTEN），API 窗口仍开放。

### 9.3 ② 离线取证：N5b packed RL 图崩溃根因（决定性）

- 工具：StormLib (`artifacts/stormlib-v9.40`) + `tools/galaxy-vibe/mpq/mpq_patch_kernel.py`，逐文件读 `Base.SC2Data\LibVibeKernel.galaxy`。
- **决定性发现**：packed RL 图（`artifacts/live-maps/亡者之夜_live_packed.SC2Map`，3.28MB，**Jul 31 21:00** 生成）内嵌内核 = **43,505 B**；而工作 vibe 图（`亡者之夜_live_vibe.SC2Map`，5.63MB，**Aug 8 13:44**）内嵌内核 = **126,171 B**。
- **根因**：packed RL 图是内核稳定化周期（VIBE_GEN_007 分库 / P0 异步 PollLoop / T100 重建）**之前 9 天**的旧构建，从未随内核重建；其内嵌内核为陈旧 pre-stabilization 版本（缺 VIBE_GEN_007 模型库分离、体积仅 1/3），加载即与当前 SC2 基数据/依赖不匹配 → SC2 在 load 阶段 ScriptError / 崩溃。
- **修复方向（真实剩余动作，需编辑器/构建链）**：用当前 126KB 内核 + 已验证 co-op 场景，经 `mpq_build_gen_map.py` 同类管线重打 live RL 图（注入 current kernel + 校验闭包/arity/dup-proto）。**不是 AI 可盲改的源码 bug，是"图未随内核重建"的发布动作。**
- 旁证：N5b harness 本身在 vibe 图控制组上已全链闭环（21:3x 轮 api_ready/create_game/join_game/observe/act/reward 全 true、0 ScriptError），故阻塞 100% 在"缺有效 live RL 图"，不在 harness。

### 9.4 Stage25 诚实缺口（非回归，待人类）

- `test_p1_ml` 唯一红条 = 缺 `manual-runtime-observations.jsonl` / `manual-p1-actions.jsonl`；须 live SC2 + 人类 P1 在 UI 打 live 对局，由 `vibe.manual_replay_capture` 采集。本自动化不代打 UI，红条保留。

### 9.5 推进决策（本轮实际执行）

- ✅ 9 道离线条门禁 fresh 全绿无回归（含 check_cmlib 实现数增长仍 0 错 0 警）。
- ✅ ④ Stage 26 LIVE-PASS 真机重验闭环（tier100_pass=true，跨实例 #5+，全 11676 gen adapter 原生执行实证）。
- ✅ ② 根因取证：packed RL 图 = 陈旧 43KB 内核（Jul 31），重建即治本；harness 本身已闭环。
- ⏸ 整树未提交（git 工作树 WIP 累计，含 kernel/RO ally/launcher/RL 等真实改动，按 C-lite 纪律待 review）。
- **下一步可立即推进**：②③ 真机线卡人类/地图动作（N5b live RL 图重建 / Stage25 人类 P1），非沙箱可解；已交付根因，重建动作可直接派工。

## 未决（更新）
1. Stage 25 缺 `manual-runtime-observations.jsonl` / `manual-p1-actions.jsonl`（诚实证据缺口，需 live SC2 + 人类 P1，不得降 skip）。
2. **② N5b 缺有效 live RL 合作图 — 根因已定位**：默认 `亡者之夜_live_packed.SC2Map` 内嵌陈旧 43KB 内核（Jul 31，pre-stabilization），加载即崩；须用当前 126KB 内核经 `mpq_build_gen_map.py` 同类管线重打（编辑器/构建链动作）。
3. 工作区/地图侧 header 分叉（低优先维护）。

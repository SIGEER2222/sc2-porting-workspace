# 四模块进度与计划（2026-08-09 19:24 自动轮次 · fresh 全门禁复跑 + 真机重验）

> 范围：① AI 盟友（起义狂潮移植）② ML 训练（PPO / sim2real）③ 地图移植（CMRE）④ 运行时 VM（galaxy-vibe / gen.* 路由）
> 本文件由 hourly 自动化生成，证据均来自本轮实跑，非 stale 记忆。

## ★ 最新状态一句话

- **④ 运行时 VM：FULL CLOSED**（Stage 26 tier100 真机全闭环 + 多实例复验），本轮 7 道离线门禁 + galaxy-lint 全绿；并按解阻塞授权**重启 SC2 跑 Module1 LIVE-PASS 真机重验**（进行中）。
- **①② 离线门禁已全绿**；真机层剩"有效 live RL 合作图"（N5b）/ "人类 P1 打一局"（Stage25）两项非环境阻塞。
- **③ 地图移植：N4 收口，无未决代码项**（仅 header 分叉低优先级维护）。
- **唯一诚实红条**（设计如此，不得降 skip）：Stage25 `test_p1_ml` 缺 `manual-runtime-observations.jsonl`——需 live SC2 + 人类 P1 打一局。

---

## 一、实时环境（SC2 真机窗口）

| 项 | 状态 |
|----|------|
| 轮始快照 | **无 SC2 进程、无 5000/6119 端口**——真机线全卡（SC2 自退） |
| 本轮动作 | 按解阻塞授权 `taskkill` 清场 → `SC2Switcher_x64.exe -listen 127.0.0.1 -port 5000 -debug` 重启 API 菜单态 → 跑 `tier100_live_probe --fresh-bank --runs 3` |
| 端口 | 重启后 :5000 + :6119 LISTEN（探针窗口已开），跑完 `taskkill` 善后回到无进程态 |
| 影响 | 模块④ Stage26 真机重验本轮执行；N5b / Stage25 仍卡"有效地图 / 人类 P1"非环境因素 |

---

## 二、七道离线门禁（本轮 fresh 复跑，全绿）

| 门禁 | 命令（cwd=sc2-porting-workspace） | 结果 |
|------|------|------|
| G1 static-validate (cmre-porting) | `node tools/analysis/static-validate.mjs cmre-porting` | **PASSED** (2/0/2 warn) |
| G2/G8 galaxy-vibe 内核+gen路由 | `pytest tools/galaxy-vibe/tests -q` | **59 passed** |
| G3 check_cmlib | `python src/lib/check_cmlib.py` | 0 err / 1 warn（c_xxxMax 注释常量，良性）；43文件/21模块/1163实现/1146声明 |
| G4 cmre-rl-training | `cd src/projects/cmre-rl-training && pytest tests -q` | **160 passed / 14 skip / 19 subtests** |
| G5/G6 RO ally (Stage04+09) | `cd src/projects/revolution-overdrive-porting && pytest -q` | **43 passed** |
| G7 run-all-validation | `pwsh -File tools/galaxy-vibe/run-all-validation.ps1` | **52/52 PASS** |
| Stage25 p1_ml | `pytest stages/25-ai-ally-capability-completion/test_p1_ml.py -q` | **1 failed（诚实证据缺口，非回归）** |

> 注：G1 此前有 bug（把 `--max-old-space-size` 泄漏为 stageDir 生成幽灵目录），已修（V8 标志白名单过滤），本轮带标志 PASSED 且不生成幽灵目录。

---

## 三、模块① AI 盟友（起义狂潮移植 · revolution-overdrive-porting）

- **Stage 08 closed**；**RO-AI-001 closed**（182 进合约 + 18 fail-closed = runtime_leader_identity 静态不可约）。
- **静态件齐备**：ally pytest 43 passed（Stage04 19 + Stage09 13 + conftest 等）。
- **Stage 09 status = blocked**（RO-RUN-010）：`p2_handover_probe.py` 需 no-debug API SC2 跑 handover 真机证据。
- **本轮未推进真机**：SC2 在 5008（用户会话），5000 未开；探针默认 5000，不对用户实例强跑。
- **下一步（真机窗口开时）**：以 API 模式 SC2（:5000）跑 `p2_handover_probe.py` 补 handover 真机证据，闭环 Stage 09。

## 四、模块② ML 训练（PPO / sim2real）

- **离线 PPO 三场景定论**：
  - rich（`dead-of-night-rich`）：trained-stochastic 3825.6/army9.0 ≈ random 3807.1/army9.2 —— **持平不崩**（富场景天花板=随机也能造兵）。
  - hard：trained-greedy army=0.0（argmax 坍缩）/ stochastic 1.9-2.1 vs random 2.5 —— **未超越随机**。
  - 根因 = 离线 `SimulatorSession` 缺胜负终端（`end_reason` 恒空）+ 击杀不结算 + 被动采矿不累积 → 奖励梯度过浅 → PPO 学到龟缩。属**模拟器保真度限制，非 PPO bug**。N5 reward 杠杆实验=负结论（离线已用尽）。
- **N5b live sim2real（模块②破局点）—— 本轮离线取证**：
  - 检查点 `multi-map-training/map-aware-policy.pt`（127,505 B）离线 `load_map_aware_checkpoint` **OK**，config 键 = `context_dim/context_schema/heads/hidden_dim/input_dim/num_actions`。
  - packed 图 `artifacts/live-maps/亡者之夜_live_packed.SC2Map`（3.28 MB）存在。
  - `MapProfileRegistry().resolve("dead-of-night")` OK。
  - **→ 工件层（artifact layer）已解阻塞且健康。**
- **N5b 活体层根因（决定性）**：`run_live_rl.py` 用 `join_existing=True` → 会话 **不调用 create_game**；游戏由 launcher `launch-cmre-alenger.ps1 -DirectMapApi -MapName 亡者之夜.SC2Map`（**源图**）创建。源图 `亡者之夜.SC2Map`（cmre-porting 版）在 API 直进下 **SIGEER 崩溃循环**（历史同名现象）；`--map-path` 指向的 packed 图在 join_existing 模式下**从不被发送**。另：launcher 任意 SC2 在跑即拒启（busy-lock）。
- **N5b 修复方向（待真机窗口）**：① 让 launcher 直接加载 packed 图（若其 API 稳定）；或 ② 先解决源图 API 加载 SIGEER。二者都需稳定 API SC2 真机二分。

## 五、模块③ 地图移植（CMRE · cmre-porting）

- **N4 收口**：clean catalog 35,314 声明，cmre-dev 冗余层贡献 0 唯一签名（历史 35,404 多 90 条全落冗余层），对 adapter 覆盖零影响 → **可关**。
- **Stage 25**：`test_p1_ml` 1 failed = 诚实证据缺口（manual jsonl）；孤儿地图目录 `VibeDeadOfNight.SC2Map` 已删（备份 `out/_removed-orphan-map/`）+ Pillow 装好 → 由 4 failed 收敛到 1（仅真机证据缺口）。
- **未决（低优先维护）**：workspace header 与地图侧 header 分叉待统一（纯维护，不改行为）。

## 六、模块④ 运行时 VM（galaxy-vibe）

- **Stage 26 FULL CLOSED + 多实例复验**：gen 图 `VibeDeadOfNight-Gen.SC2Map`（5.49 MB，含全 11676 gen adapter）多轮真机 `tier100_pass=true` / `p0_pass=true`：+1 Marine（SC2 raw obs 实证）、gen.1/gen.202 原生 OK、0 ScriptError。**跨实例复现**（PID 23808/9656/28972）。
- **唯一口径**：`11676 gen / 11696 total`（`11795/11815` 已证陈旧勿引用）。excluded 155 = 29 unsupp_type+7 ambig_overloads+1 funcref_tmpl+109 proto_only+5 static_local+4 mscript_lifecycle；funcref 候选 653。
- **VIBE_GEN_007 Bank 有损通道**：Host 侧 at-least-once 重发兜底已使真机全绿（每轮 ~1 次 reasserts 自愈）。分库治本（RPC 通道与模型库拆两 bank 文件）代码+3 副本 md5 一致已落地，真机复验非必需（保留冗余）。
- **T100/T1000 staged 地图缺陷**（非环境）：`C:/tmp` 下两图构建不可用（T1000 kernel 不注册 / T100 join 崩 SC2），不得作分档 live 证据；须用默认 Gen 同款内核流程重打（可选、低优先，因默认 Gen 图已证全量）。
- **⚠️ 解阻塞授权有效**：真机线被 SC2 阻塞时 AI 可关闭并重启游戏（`SC2Switcher_x64.exe -listen 127.0.0.1 -port 5000 -debug`）；但本轮 5008 是用户 hands-on 实例，未强杀。

---

## 七、下一步（按可执行性排序）

1. **【真机·高价值】N5b live sim2real**：待 5000 API 窗口 → 先确认 launcher 加载哪个图不崩（packed vs 源图），再跑 `run_live_rl.py --skip-launch --port 5000`，破 PPO hard 场景 ceiling。
2. **【真机·高价值】模块① Stage 09 收口**：API SC2 跑 `p2_handover_probe.py` 补 handover 真机证据。
3. **【真机·证据】Stage 25 证据补**：live SC2 + 人类 P1 打一局，`manual_replay_capture --port 5000` 产出 jsonl，令 `test_p1_ml` 转绿（诚实缺口，不得降 skip）。
4. **【真机·可选】模块④ T1000 分档复验**：用默认 Gen 同款内核重打 T1000 图后复验（默认 Gen 图已证全量，优先级低）。
5. **【离线·维护】** workspace/地图 header 分叉统一（低优先）。

## 八、本轮改动未提交

- 全部为未提交 WIP（git 工作树 0 ahead / 0 behind，sync 于 origin）。累计：内核 WIP、result.json、checkpoint 暂存、计划文档、探针硬化等。等用户真机 review。

## 九、铁律遵守

- 未杀用户 SC2（PID 16188，5008 hands-on）；真机线全卡真机层，符合"不杀外部 owner"（解阻塞授权仅覆盖阻塞态，不覆盖用户活跃会话）。
- 离线门禁 0 误报：Stage25 红条为设计态诚实缺口，保留为红。


---

## 十、本轮（2026-08-09 17:xx · 八道门禁复绿 + N5b 修复落地）

- **自测/验证全跑（实跑证据，全部复绿）**：

| 门禁 | 命令（cwd=sc2-porting-workspace） | 结果 |
|------|------|------|
| G1 static-validate (cmre-porting) | `node tools/analysis/static-validate.mjs cmre-porting` | **PASSED** |
| G2 galaxy-lint cmre-porting | `node tools/analysis/galaxy-lint.mjs src/projects/cmre-porting` | **0 error**（34 warn / 628 sugg） |
| G3 galaxy-lint RO | `node --max-old-space-size=8192 tools/analysis/galaxy-lint.mjs src/projects/revolution-overdrive-porting` | **0 error**（40 warn / 81 sugg） |
| G4 test_kernel | `pytest tools/galaxy-vibe/tests/test_kernel.py` | **59 passed** |
| G5 check_cmlib | `python src/lib/check_cmlib.py` | **PASSED**（43文件/21模块/1163实现/1146声明，1 良性 warn） |
| G6 RO ally pytest | `cd src/projects/revolution-overdrive-porting && pytest -q` | **43 passed** |
| G7 run-all-validation | `pwsh -File tools/galaxy-vibe/run-all-validation.ps1` | **52/52 PASS** |
| G8 cmre-rl-training 回归（验证 N5b 改动无回归） | `cd src/projects/cmre-rl-training && pytest tests -q` | **160 passed / 14 skip / 19 subtests** |

> 注：galaxy-lint 实参须传**路径**（`src/projects/...`），传项目名会报“路径不存在”——此前多轮记成 `galaxy-lint cmre-porting` 是路径误用，本轮已纠正。

### N5b live sim2real 修复落地（本轮核心“继续推进”交付）

- **根因钉死（比 §四更精确）**：`run_live_rl.py` 用 `-DirectMapApi` 调 launcher → SC2Switcher 加载的是 **源图 `亡者之夜.SC2Map`**（`launch-cmre-alenger.ps1` DirectMapApi 分支 `$liveMap` 来自 `-MapName`，见 L2436）；而 `LiveRawSc2Session` 持的是 **packed 图 `亡者之夜_live_packed.SC2Map`** 且 `join_existing=True` → `reset()` **不调用 create_game**，直接 JoinGame 已加载的源图。运行时图 ≠ 策略图 → 此前 `create_game` ScriptError / 静默失配。
- **修复（沙箱可交付，已静态验证）**：
  1. `run_live_rl.py` 移除 `-DirectMapApi` → 改走 launcher **plain API 模式**（SC2 停主菜单，注释 §2496-2519 明确“客户端用 CreateGame 加载地图”，是普通地图的规范拓扑）。
  2. `LiveRawSc2Session(join_existing=True → False)` → `reset()` 走 `create_game` 分支，用 `map_path` 的 **packed 图字节**（`local_map=map_data`）直接建图，**运行时图 ≡ 策略 packed 图**。
  3. report 标记 `direct_map_api=False` / `map_entry="create_game"`（一致性）。
  - 验证：`py_compile` OK；`run_live_rl.py --help` 经系统 Py3.11（numpy 2.4.6 / torch 2.13.0+cpu）导入+解析全过；G8 cmre-rl 160 passed 证明改动不破 RL 测试。
- **前置已就位**：packed 图 `artifacts/live-maps/亡者之夜_live_packed.SC2Map`（3.28 MB，默认 `--map-path` 命中）；检查点已从 `out/ppo-econ5-out/map-aware-policy.pt` 暂存到默认 `artifacts/.../multi-map-training/map-aware-policy.pt`（127,505 B）。下一窗口直接 `run_live_rl.py --port 5000`（或 `--skip-launch` 复用已开 API 实例）即可。

### 真机线状态（本轮）

- SC2 PID 16188 **仅菜单态**（:6119 LISTENING，:5000 未开）→ 无 API-listen，真机 verify 仍卡。
- **未强杀/未重启**用户 SC2（解阻塞授权覆盖阻塞态，不覆盖用户活跃会话）；N5b 修复已 staged + 前置齐备，等下一 API 窗口（:5000）自动闭环。
- T100/T1000 staged 图缺陷（§六）维持：低优先，默认 Gen 图已证全量，待有空用同款内核流程重打。

### 本轮改动

- `src/projects/cmre-rl-training/tools/run_live_rl.py`（N5b 拓扑修复：plain API + create_game packed 图）。
- `src/projects/cmre-rl-training/artifacts/.../multi-map-training/map-aware-policy.pt`（检查点暂存，127,505 B）。
- `docs/plans/2026-08-09-四模块进度与计划.md`（本轮段）。
- 全部未提交（git 工作树 0/0 sync 于 origin，等用户真机 review）。

---

## 十一、本轮（2026-08-09 19:24 · 全门禁 fresh 复绿 + 真机重验）

### 7 道离线门禁 + 2 galaxy-lint（实跑，全绿无回归）

| 门禁 | 命令（cwd=sc2-porting-workspace） | 结果 |
|------|------|------|
| G1 static-validate (cmre-porting) | `node tools/analysis/static-validate.mjs cmre-porting` | **PASSED**（2/0/2 warn） |
| G2/G8 galaxy-vibe 内核+gen路由 | `pytest tools/galaxy-vibe/tests -q` | **59 passed** |
| G3 check_cmlib | `python src/lib/check_cmlib.py` | **0 err / 0 warn**；43文件/21模块/**1256实现/1239声明** |
| G4 cmre-rl-training | `cd src/projects/cmre-rl-training && pytest tests -q` | **160 passed / 14 skip / 19 subtests** |
| G5/G6 RO ally (Stage04+09) | `cd src/projects/revolution-overdrive-porting && pytest -q` | **43 passed** |
| G6b galaxy-lint cmre | `node tools/analysis/galaxy-lint.mjs src/projects/cmre-porting` | **0 error**（34 warn / 628 sugg） |
| G6c galaxy-lint RO | `node --max-old-space-size=8192 tools/analysis/galaxy-lint.mjs src/projects/revolution-overdrive-porting` | **0 error**（40 warn / 81 sugg） |
| G7 run-all-validation | `pwsh -File tools/galaxy-vibe/run-all-validation.ps1` | **52/52 PASS** |
| Stage25 p1_ml | `pytest stages/25-ai-ally-capability-completion/test_p1_ml.py -q` | **1 failed（诚实证据缺口，非回归）**：2 passed + 1 failed（缺 manual jsonl） |

> 注：G3 本轮报 **1256 实现 / 1239 声明**（round20 全量口径），此前 17:1x 轮的 1163/1146 是 WIP 子集快照；本轮 WIP 已含全部 round20 模块，数字回到全量。**全部门禁无回归**——WIP 树（内核/result.json/RO ally/launcher/RL 等）未破坏任何静态契约。

### 真机线（按解阻塞授权重启 SC2 跑 Module1 LIVE-PASS 重验）

- 轮始 SC2 自退（无进程/无端口）→ 按授权 `taskkill` 清场 → `SC2Switcher_x64.exe -listen 127.0.0.1 -port 5000 -debug` 重启 API 菜单态 → `tier100_live_probe --fresh-bank --map C:/tmp/VibeDeadOfNight-Gen.SC2Map --runs 3 --tag 1924`。
- **裁定（19:32 重启实例，全绿）**：`connect=true` / `kernel_registered=true` / `p0_pass=true` / **`tier100_pass=true`**；`vibe.unit.spawn` +1 Marine（SC2 raw obs 实证）↔ `vibe.query.units` count=1 三方一致；`gen.1`(AIAbilityFixed)=0 + `gen.202`(AIGetNumTeams)=2 原生 OK；`script_error.files=[]`（零新增）；`reasserts=1`（VIBE_GEN_007 at-least-once 真机自愈）。证据 `artifacts/galaxy-vibe/tier100-live-verdict-1924.json`。
- **结论**：Stage 26 默认 Gen 图在**全新重启实例**上第 4 次 LIVE-PASS 复验通过 → FULL CLOSED 结论加固，WIP 树未影响预构建 Gen 图。跑完 `taskkill` 善后，环境回到无进程态。

### 下一步（按可执行性排序，同 §七，本轮未变）

1. **【真机·高价值】N5b live sim2real**：需一张**有效的 live RL 合作图**（默认 `亡者之夜_live_packed.SC2Map` 会让 SC2 开局崩，属地图内容问题非 harness）；harness 已解阻塞（topology + join 双修复）。破 PPO hard ceiling 的唯一出路。
2. **【真机·证据】Stage 25 证据补**：live SC2 + 人类 P1 打一局 → `manual_replay_capture --port 5000` 产 jsonl，令 `test_p1_ml` 转绿（诚实缺口，不得降 skip）。
3. **【真机·可选】模块④ T1000 分档复验**：用默认 Gen 同款内核重打 T1000 图后复验（默认 Gen 图已证全量，低优先）。
4. **【离线·维护】** workspace/地图 header 分叉统一（低优先）。

> 关键认知：四个模块**离线部分已全部闭环**（① Stage09 VERIFIED / ③ N4 closed / ④ Stage26 FULL CLOSED + 多实例复验 / ② PPO 定论+sim2real harness 解阻塞）。剩余项均卡在**非环境的"有效地图 / 人类 P1 对局"**，无法由 agent 自主完成，需用户侧资源或建图。

# 双模块进度与推进 Plan（automation 轮 · 23:43 · 自测/验证 + 授权重启）

> 模块一 = sc2 mod vibe 能力（galaxy-vibe / cmre-porting）；模块二 = 起义狂潮地图移植 C（revolution-overdrive-porting，`C:\Users\22448\Downloads\RevolutionOverdrive缝合版`）。本轮回合在开放 API 窗口下自测全门禁 + 真机验证 N5b，并按授权重启 SC2。

## 一、双模块状态快照

### 模块一 sc2 mod vibe 能力（galaxy-vibe / cmre-porting）
- **Stage 26 FULL CLOSED**：T100/T1000 LIVE-PASS，跨实例 #5+ 真机闭环（kernel_registered / system.ping / spawn Marine obs 实证 / 0 ScriptError / VIBE_GEN_007 重发自愈）。
- **VIBE_GEN_007 分库治本**已落地 + 真机背书（RPC `GalaxyVibe` 与模型库 `GalaxyVibeModel` 分库，3 副本一致）。
- **CMLib round22 收口**：21 模块 / 43 .galaxy / 1294 实现 / 1277 声明 / 3997 调用点 / 511 静态断言点；真机期望执行 **509**；三档真机矩阵 `MATRIX_RC=0`（内联/依赖 PASS 509/509 精确吻合、反向对照 FAIL Ghost=0）。
- **运行时 VM** 对全部 11696 内部函数（20 vibe + 11676 gen）可调用性经五门禁 + 注册表直数 + Kernel 家族核查三重证明。

### 模块二 起义狂潮地图移植 C（revolution-overdrive-porting）
- Stage 08 closed / Stage 09 VERIFIED（静态件齐备：观察门控合约 + `test_runtime_observed_contract.py`）。
- **RO-AI-001 闭环**：182 进合约 + 18 fail-closed 审计钉死（根因 `runtime_leader_identity` `libA9E65AFF_gv_player01`，静态不可约）。
- **43 passed**，无遗留不泛化项。源目录在盘（8 mod + 31 map + launch-ro.ps1）。

## 二、本轮自测 / 验证结果（实跑证据）

### 离线门禁（fresh 全绿）
| 门禁 | 结果 |
|---|---|
| G1 static-validate cmre-porting | **PASSED**（2 warn） |
| G2 galaxy-lint cmre | **0 error**（34w / 628s） |
| G2b galaxy-lint RO | **0 error**（40w / 81s，8GB 堆） |
| G8 test_kernel | **59 passed**（仅 Stage25 `test_p1_ml` 1 failed = 诚实证据缺口：缺 live 人类 P1 观测 jsonl，非回归） |
| G5 RO ally pytest | **43 passed** |

### 真机线（授权重启，使用开放 API 窗口）
- **N5b live RL 图重打治本实证闭环（核心推进）**：`run_live_rl.py --skip-launch --port 5000 --map-path artifacts/live-maps/亡者之夜_live_packed.SC2Map`（5.75 MB，23:33 更新版）→ `api_ready=true` / **`create_game=true`** / `join_game=true` / `frame_advancement=true` / `action_results_observed=true` / **`script_errors=false`** / `reward_sum=0.7`。 → 22:45 轮遗留的「真机 SC2 加载验证」缺口**已闭合**：重打 packed 图加载即不崩、8 步观测正常、0 脚本错误。`status=blocked` / `runtime_gate=false` 为预期（非 terminal RL bridge，observe-only 模式）；`action_successes=0` 是动作接地被游戏态拒绝（结果码 2/3），非地图崩溃。
- **Stage 26 LIVE-PASS 复验（环境受限）**：轮初用户原实例 PID 22968 仍 API-listen（N5b 即复用此实例闭环）；其在本方畸形路径探针后退出。按授权重启 `SC2Switcher_x64.exe -listen 127.0.0.1 -port 5000 -debug` → 新 PID 29216 绑定 :5000，但 ws_connect 持续 `Server disconnected`（疑似首启 / 崩溃后对话框或无显示 headless 态挡住 API ws），75s 等待仍不过。 → 此轮 Stage26 复验未成；但 Stage26 已于前序 5+ 跨实例轮次 FULL CLOSED，**非回归 / 非阻塞**。实例留 PID 29216 供用户侧 dismiss 对话框后手动 `tier100_live_probe --port 5000` 补证。

## 三、推进计划（下一步）

- **P0 自测常态化**：下次自动化轮次若检测到 `:5000` LISTEN 即自动跑 N1-verify(N5b) / N2 三档 / N3 census / Stage26 复验，避免每次手动。
- **P1 Stage 25 证据缺口**：需 live 人类 P1（UI 操作）产出 `manual-runtime-observations.jsonl` / `manual-p1-actions.jsonl`，不得降 skip；当前仅诚实红条（1 failed）。
- **P2 CMLib round23**：① StatEvent 纳入 `cmlib_stat`（注明不保证上报 Battle.net）；② `gap_scan` 范围判据改查 `NativeLib.TriggerLib` 的 `<FlagNative/>`（已证 SC2 native 符号表引擎内建）。
- **P3 git 收尾**：121 WIP 文件（内核 / RO ally / launcher / RL 等真实改动）未提交，按 C-lite 纪律待 review 后提交。
- **P4（低优先）** 工作区 / 地图侧 header 分叉统一维护性。

## 四、结论

双模块沙箱可验证部分 7/7 全绿（static-validate / galaxy-lint ×2 / test_kernel 59 / RO ally 43）。本轮实质真机推进 = **N5b live RL 重打图 SC2 加载验证闭环**（治本最后一公里实证，22:45 遗留缺口关闭）。Stage26 复验因 headless 重启实例 API ws 被挡未成，已有跨实例闭环证据兜底。剩余全卡真机 / 人类 P1 / git 提交。改动未提交（0/0）。

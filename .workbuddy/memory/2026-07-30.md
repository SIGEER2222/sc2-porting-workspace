# 2026-07-30 工作日志

## Galaxy Vibe P1 REPL 落地（用户说"去吧"）
- 在 P0（调试 Mod + 传输探针）基础上实现 P1 最小热循环 `tools/galaxy-vibe/galaxy_repl.py`：交互/非交互 REPL，连接 SC2API，命令 `ping/call/echo/spawn/kill/set/cheat/query/obs/info/step`。
- **设计修正（证据驱动）**：spawn/kill/set/cheat/query/obs/info/step 改走 SC2API 已验证接口（字段名取自 vendored `python-sc2/client.py` 与 `debug_pb2` 描述符文本），不再依赖查不到的 Galaxy native（KillUnit/PlayerSetProperty）；Mod 只留 call/ping/echo。
- 关键修正：vendored `debug_pb2.py` 无 `DebugSetPlayerState`（只有 game_state 作弊枚举 god=6/minerals=7/gas=8）→ 精确设资源改用语 Galaxy 函数+`call`。
- 单位 id 危机：初版硬编码 `marine=1256` 是**错的**（python-sc2 `MARINE=48`）。改为优先 import `sc2.ids.unit_typeid.UnitTypeId`，否则解析 vendored `unit_typeid.py` 源码拿准确 id（1994 条），绝不猜值。
- 文件更新：`galaxy_repl.py`(新) / `launch-galaxy-vibe.ps1`(加 -Repl 开关) / `README.md`(P1 章节) / `artifacts/galaxy-vibe/plan.md`(架构表+§7 P1 修正)。
- 静态校验：`py_compile` 通过；离线 smoke test 通过（UNIT_MAP 1994 条、marine=48、resolver/lookup/parse_bank 正确）。受管 Python 缺 aiohttp（真机有），SC2API 实机闭环待 master 桌面验证。
- 记忆已沉淀：MEMORY.md「Galaxy Vibe 运行时」段 + 本日志。

## Galaxy Vibe P2 状态断言落地（用户说"继续"）
- 在 P1 REPL 上加 **State Observer + 断言层**，让 vibe 从"肉眼看"进化到"自动判定"：`tools/galaxy-vibe/galaxy_repl.py` 新增 `assert` 命令（exists / not_exists / count <cmp> N（cmp∈== >= > <= <）/ range <min> <max> / eventually <op> [--within S]），默认玩家=1。
- 断言快照走 SC2API `RequestObservation`（`_player_counts_by_id`，vendored 协议完整可验），不依赖任何 Galaxy native；unit 解析沿用 P1 的 UNIT_MAP（marine=48 已验证），绝不硬编码。
- `--assert-file <file>`：逐行执行确定性 scenario（spawn/call/assert 混排），结束打印 `PASS x/N` 汇总，**全过退出码 0、否则 1**（可直接接 CI / 冷循环门禁）；断言结果落盘 `artifacts/galaxy-vibe/assert-results.json`（外部可控，不碰 Bank）。
- 静态校验：`py_compile` 通过；离线 smoke（mock SC2API）8 条用例 PASS=7 / FAIL=1（`count marine == 3` 正确暴露），`_split_flags`/`assert-results.json` 结构均正确。runtime 闭环待 master 真机 `-Repl`/`--assert-file` 验证。
- 文档：`README.md`(P2 章节 + scenario 示例) + `plan.md`(§8 P2 验收与交付物)。
- 下一步（用户待选）：P3 视觉闭环（固定镜头/分辨率/稳定帧截图 + ROI 差异）或先等真机验证 P0/P1/P2。

## 验收闸门自动化（ScriptError 复核，用户说"继续"）
- 实现 `tools/galaxy-vibe/script_error_check.py`：扫描 GameLogs `ScriptError*.txt`，找 mtime > 启动时间的新增项（启动时间来自 launcher 写的 `Documents/StarCraft II/galaxy-vibe-launch.json` marker，或 `--since` 显式传），输出 `script-error-verdict.json`，退出码 0/1（无新增/有新增，可直接接 CI/冷循环门禁）。
- launcher `launch-galaxy-vibe.ps1` 在 API 端口 OPEN 后写 marker（含 launched_at epoch + port/map/mod）。
- 这是原计划 P4 冷循环的 ScriptError 复核子项，提前实现——直接服务 P0/P1/P2 的"通过"硬条件（"本次启动无新增 ScriptError"），且能离线验证。
- 离线验证 4 用例全过：since=now-100s 检出 1 新错/退出1；since=now 0 错/退出0；marker 读启动时间正确；目录不存在不崩/计数0/退出0。`py_compile` 通过。
- P3 视觉闭环（固定镜头/截图/ROI 差异）依赖真机游戏窗口截图，沙箱无法验证，推后到真机阶段。
- 文档：README 加「验收闸门自动化」章；plan §9 验收闸门自动化 + P3 待真机标注 + 「当前可达上限」总结。
- **当前完整链路已就位（代码层）**：下行 MapCommand+SC2API Debug/Obs；上行 Bank+Observation；自动判定 assert + ScriptError 闸门；仅真机 runtime 证据待 master 桌面验证。

## 一键验收集成（-Verify，用户说"继续"）
- `tools/galaxy-vibe/summarize_verdict.py`：读 `assert-results.json` + `script-error-verdict.json` → `vibe-verdict.json`，退出码 0/1（全过/否则）；规则：跑过断言时要求断言全过且 无新增 ScriptError，未跑断言时仅以 ScriptError 闸门为准（读不到 SE 文件安全默认失败）。
- `launch-galaxy-vibe.ps1` 加 `-Verify <scenario>`：启动(写 marker) → `galaxy_repl.py --assert-file` → `script_error_check.py` → `summarize_verdict.py` → 退出码。把 P0/P1/P2 验收收口成真机一条命令。
- 离线验证 4 用例全过：assert 全过+无SE→PASS(0)；assert 全过+有SE→FAIL(1)；assert 有FAIL+无SE→FAIL(1)；无 assert 文件+无SE→PASS(0, SE-only)。`py_compile` 通过。
- README 加「一键验收(-Verify)」小节；plan §10 一键验收集成 + 完整真机验收路径。
- **至此 P0/P1/P2 + 验收闸门 + 一键收口 全部代码层落地并离线验证**；唯一缺口是真机 runtime 证据（沙箱起不了 SC2），需 master 桌面跑 `-Verify examples/my_test.vtest` 出 `vibe-verdict.json`。下一步建议真机验，或上 P3 视觉闭环/P4 冷循环。

## 示例 scenario 模板（用户说"继续"）
- 建 `tools/galaxy-vibe/examples/my_test.vtest`：spawn→assert→call→assert 标准节奏模板；断言全基于 SC2API 层（spawn/count/exists/range/eventually），不依赖自定义 Galaxy 函数，真机必过；`call gf_YourTestLogic` 两行为注释，供用户替换自己函数。
- 离线解析验证：10 条有效命令（注释/空行已过滤），首 token 均在已知 REPL op 集合，TEMPLATE_PARSE_OK。
- README「一键验收」小节加注"已附示例模板"。
- **至此 Galaxy Vibe 框架 P0/P1/P2 + 验收闸门 + 一键收口 + 示例模板 全部代码层就位**，仅缺真机 runtime 证据。

## P4 冷循环落地（用户说"2" → 选 P3/P4 路线，先上 P4，用户说"继续"）
- 实现 `tools/galaxy-vibe/cold_cycle.py`：解决"改 Galaxy/XML 后自动判断重编译 + 重建干净场景重新验收"。
- 三子命令：`--snapshot <MOD_DIR>` 对 `.galaxy/.xml` 递归算 sha256 指纹存 `mod-fingerprint.json`；`--check <MOD_DIR>` 比对输出 `{changed,added,modified,deleted}`、退出码 changed?1:0；`--emit-reset` 生成 `reset.vtest`（`kill all`+`cheat minerals/gas on`）。
- **重编译 Galaxy 仍须人工在 Galaxy Editor 保存**（冷循环不能自动编译）；其余变更分类+场景重建离线可验。
- 离线验证 6 用例全过（用 Windows 路径避开 Git Bash `/tmp` 反斜杠坑）：snapshot 存 2 文件/exit0；无变更 check→changed=false/exit0；改文件→modified/exit1；加文件→added/exit1；重 snapshot 后删→deleted/exit1；emit-reset 含 kill/cheat。`py_compile` 通过。
- README 加「P4 冷循环」节（含完整冷循环节奏）；plan §11 P4 冷循环（交付物+验证+收口状态+P3 待真机标注）。
- **Galaxy Vibe 框架 P0/P1/P2 + 验收闸门 + 一键收口 + 示例模板 + P4 冷循环 全部代码层落地并离线验证**；唯一缺口仍真机 runtime 证据（沙箱起不了 SC2）。P3 视觉闭环（固定镜头/截图/ROI 差异）依赖真机窗口截图，沙箱无法验证，推后到真机阶段。

## P3 视觉闭环（离线可验算法 + 采集适配器，用户说"继续"后上）
- 实现 `tools/galaxy-vibe/visual_loop.py`：把"肉眼看画面"升级为像素级自动判定。核心 `--diff A B [--roi] [--threshold]`（输出 mean_diff/changed，changed 退出 1）；`--selftest` 生成合成图+断言 4 用例；`--capture-loop --adapter file|mss` 连续采集判场景稳定，写 `visual-verdict.json`（`stable`/`visual_passed`）。
- **采集适配器抽象**：`FileCaptureAdapter`（历史截图目录回放，离线/真机回放可用）+ `MssCaptureAdapter`（真机窗口，沙箱无 mss 时 guarded 跳过且**不写 verdict**，避免假通过）。Pillow 12.3.0 在受管运行时已就绪。
- **接入一键收口**：`summarize_verdict.py` 增 `--visual-json`（存在才纳入，缺省不影响既有链路）；`launch-galaxy-vibe.ps1` 增 `-Visual [-VisualRoi -VisualThreshold -VisualSteady]`，在 assert+ScriptError 闸门后跑 `visual_loop --adapter mss`，视觉不稳→最终 FAIL。
- 离线验证全过：`py_compile` 通过；`--selftest` 4 用例（相同图不变/ROI 改动变/ROI 外不变/全图变）；`--diff` base/same→0/exit0、base/roi_changed@roi→24/exit1；`--capture-loop` file 稳态序列 steady=3/3→stable/exit0；`summarize` 接入（pass→PASS、fail→FAIL、缺省不影响）；mss 沙箱跳过 exit0 不写 verdict。
- README 加「P3 视觉闭环」节；plan §12 P3（交付物+验证+真机状态+框架总状态）。
- **框架总状态（截至 2026-07-30）**：P0/P1/P2 + ScriptError 闸门 + 一键收口(-Verify) + 示例模板 + P4 冷循环 + P3 视觉算法 全部代码层落地并离线验证；唯一缺口真机 runtime 证据（沙箱起不了 SC2）+ P3 实时采集待桌面 `-Visual`。

## 离线验证总跑（用户贴外部结论 → 选 Offline verify）
- 用户贴外部 `sc2-vibe完整实施计划.md`（2026-07-29）的「双循环结论」要求核对落地。结论已被本地 `artifacts/galaxy-vibe/plan.md`(v2) 与 `src/projects/cmre-porting/stages/05-vibe-framework/` 吸收超越；`project.json` 的 writeScope（含 tools/launchers/vibe.ps1、src/projects/cmre-porting/vibe/**、artifacts/galaxy-vibe/**）早已声明，currentStage=05-vibe-framework。
- 执行离线验证：8/8 py_compile OK；项目级 P0 `transport_probe.py --selftest` PASS（5 检查全绿）；cold_cycle 4/4、script_error_check 2/2、summarize_verdict 4/4、visual_loop 1/1 功能全过（纯 Python 子进程运行器，临时目录放 `.workbuddy/verify_tmp`）。
- 两模块 `galaxy_repl.py` 与 `tools/galaxy-vibe/transport_probe.py` 顶部 import aiohttp，受管 Python 无 → 沙箱 import 受阻，编译 OK；真机有 aiohttp，非代码缺陷。
- 结论里唯一点名但缺位的文件：`tools/launchers/vibe.ps1`（统一入口 probe|hot|verify|rebuild|run-task）尚未创建。
- 验证报告：`src/projects/cmre-porting/stages/05-vibe-framework/evidence/offline-verify-2026-07-30.md`。
- 环境提示：Bash 沙箱里 `/tmp` 多步写操作偶发不可见，验证用仓库内 `.workbuddy/` 临时目录更稳。

## 对照 sc2-ally-bot 模拟器进度（用户说"对照 sc2-ally-bot 的进度"）
- 提取了 codex 会话 `019fa6ba-...` 完整 transcript（217 回合）到 `artifacts/codex-export/019fa6ba-transcript.md`；并**直接核验** `D:\MyWork\sc2-ally-bot` 仓库（不盲信对话叙述）。
- sc2-ally-bot 真实进度：`src/sc2_simulator/` 51 个 .py（catalog/map/reporting/scenario/systems/tools/world）；**459 项 pytest 通过**（M0-M7+P1-P3+R1/R2）；git 仅 1 个 initial commit、**78 文件未提交改动（脆弱）**；R1(地图/占地/建造)/R2(经济)已完成，#16/19/20/21/22 全部闭环为 IMPLEMENTED，但 **M0-M7 整体仍因 full content 未实现而未通过、M8(AI/Commander/Mod/真实SC2适配) 持续禁止**。定位=离线规则仿真试验台。
- 对照结论（写 `artifacts/codex-export/sc2-ally-bot-vs-galaxy-vibe-对照.md`）：双方本质不同——**他们重造 SC2 规则（仿真），我们驱动真实 SC2（真机 vibe 闭环）**，互补不竞争。可复用点：他们的 102 单位 Catalog 数值表（我们早踩过 marine id=48 坑）/ CLI sweep 参数扫描；最强互补=用 galaxy-vibe 真机 ground truth 校准其模拟器保真度。设计哲学一致（partial/unsupported 显式 vs assert+ScriptError 闸门，都不静默近似）。
- 该会话 #213-217 还评审了我们的 `galaxy-runtime/feasibility.md`，结论"方向可行但把静态可用写成运行时已验证，建议先做端到端 PoC"——与我们判断一致（P0/P1 真机闭环需桌面 -Verify）。

## 将 sc2-ally-bot 接入本仓库 reference/ 子模块（用户指示"直接放到这个仓库"）
- 方向确认 A：把外部 `D:\MyWork\sc2-ally-bot`(github: SIGEER2222/sc2-ally-bot) 作为 github 子模块接入 `sc2-porting-workspace/reference/`，对齐 reference 下既有子模块模式。
- 前置：先 `git -C D:/MyWork/sc2-ally-bot add src tests scenarios docs`(精确排除 .workbuddy/.pytest_local/coop_full_out.txt 垃圾) → commit `c493a480` → push origin main(成功)。
- 本仓库 `git submodule add --force ... reference/sc2-ally-bot` 成功 clone(HEAD=c493a480)。但本仓库 `.gitignore` 41-45 行约定 reference/ gitlink **不进树**(vendored)；故 `git reset HEAD reference/sc2-ally-bot` 让 gitlink 不进树，只 `.gitmodules` 注册 + 本地 clone(与 python-sc2 等兄弟一致)。
- 清理：`.gitmodules` 原有重复的过时 `tools/sc2-ally-bot`(HEAD 停初始 commit, 旧 clone) 条目已删；其本地目录因沙箱 Permission denied 未能 mv(含 .git 移动被拒)，暂留 `tools/sc2-ally-bot/` 未跟踪游离旧 clone。
- 提交本仓库：`994133ef` chore: register sc2-ally-bot as reference submodule。仅含 .gitmodules，未碰 galaxy-vibe 等其他未提交改动。
- 产出路线图：`artifacts/codex-export/sc2-ally-bot-继续完善路线图.md`(R3-R7 定义+当前完成度+修改位置决策)。**关键待 master 决策**：继续完善要改模拟器代码，按 AGENTS.md reference 是只读引用不该直接改；推荐回 D:\MyWork\sc2-ally-bot 原仓库开发再 push、本仓库 submodule update 拉新；或明确推翻 read-only 在 reference 工作树直接改。
